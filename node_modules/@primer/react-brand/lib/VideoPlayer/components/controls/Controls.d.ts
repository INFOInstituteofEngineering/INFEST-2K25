import React from 'react';
type ControlsProps = {
    videoRef: React.RefObject<HTMLVideoElement>;
    videoWrapperRef: React.RefObject<HTMLDivElement>;
    totalTime: number;
    currentTime: number;
    playing: boolean;
    trackInformation?: TextTrackCueList;
};
export declare const Controls: ({ videoRef, videoWrapperRef, totalTime, currentTime, playing: playingProp, trackInformation, }: ControlsProps) => JSX.Element;
export {};
