import React, { PropsWithChildren } from 'react';
import { BaseProps } from '../component-helpers';
type CardSkewEffectProps = {
    perspective?: number;
    disableSkew?: boolean;
} & BaseProps<HTMLDivElement> & React.HTMLAttributes<HTMLDivElement>;
export declare const CardSkewEffect: ({ color, children, perspective, disableSkew, ...rest }: PropsWithChildren<CardSkewEffectProps>) => JSX.Element;
export {};
